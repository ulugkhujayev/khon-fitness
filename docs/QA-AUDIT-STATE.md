# Khon Fitness audit checkpoint

Read this file first when the user says "continue" about the 0.4.7 end-to-end audit. Then run `bash scripts/qa-audit-status.sh` once. Do not restart the audit or repeat passed flows unless code or the device changed. Keep this file current at the end of each work batch, especially before a usage limit or context boundary. The detailed findings are in [QA-0.4.7-S25-audit.md](QA-0.4.7-S25-audit.md).

## Current state, 2026-09-24

- Target: Samsung Galaxy S25 with its actual Android and One UI. No physical S25 is connected. Generic API 36 emulator at 1080 × 2340 and 420 dpi is only a logic and layout proxy; it cannot certify One UI, 120 Hz smoothness, notifications, battery, or widgets.
- Published baseline: 0.4.7 APK, SHA-256 `b3ed77524392af0cdeac81099046583049a7418f9695d844d2d5145d1d66dfbb`. Baseline evidence is in `dist/qa-047-e2e/`, which Git ignores. Do not reset or remove that directory.
- Local signed preview: `dist/khon-fitness-0.4.8-audit-preview.apk`, SHA-256 `45a9d224688f3437646b51117c728621860075a189e762764ceeaaba117cdfd8`. It is installed on the emulator. The changes are uncommitted, unpushed, and unpublished. Do not push without a fresh user message explicitly saying `PUSH`.
- Four confirmed baseline defects are repaired locally and rechecked on the installed preview: a pipe in a stretch name crashed Today; late "Open now" did not open the eating window; deleting a referenced exercise emptied a program block; archived modalities could not be unarchived. Reproductions, fixes, and evidence are in the report.
- Validation already done: 23 debug unit tests passed, signed release APK built, `lintVitalRelease` passed, `git diff --check` passed. Repeat only after new code changes. Earlier Android 9 findings do not count against the S25 target.
- Strength continuation passed: the TP v3 session survived emulator cold boot. A 60 kg × 8 rep set was marked done, survived force-stop, and appeared as 1/42 sets after reopen. Finish showed 1 of 42 and 480 kg; Save put the session in History on its start date, September 23. Evidence: `dist/qa-047-e2e/83-resume-after-cold-boot.xml`, `87-set-persists.xml`, `88-finish-prompt.png`, `91-history-saved-date.xml`.
- One new copy defect was fixed: History said "1 sets". After reinstalling the updated preview over preserved data, it says "1 set · 480 kg" in `dist/qa-047-e2e/93-history-singular-fixed.xml`. Today uses the same singular rule. The 23 unit tests, release build, and vital lint passed again. Last UI position: History with September 23 selected.
- Stretch control batch passed on the installed preview: visible 3-2-1 countdown, Pause frozen at 0:43 for three seconds, Resume, Skip to an explicit Start, natural expiry of the next stretch to another explicit Start, and navigation through all 14 phases. History recorded `1 min · 1 done · 13 skipped` on September 24. Evidence: `dist/qa-047-e2e/95-stretch-start.png`, `97-stretch-paused.xml`, `98-stretch-still-paused.xml`, `99-after-skip.xml`, `100-natural-stretch-boundary.xml`, `101-skip-sequence-*.xml`, `102-history-stretch.xml`. This did not test a full 9:30 of continuous exercise. Last UI position: History, September 24.
- Visual observation: the Elephant walks figure is readable and the bottom control fits the S25-sized viewport, but the figure is still a simplified gray mannequin and falls short of the user's muscular anatomy reference. See `dist/qa-047-e2e/95-stretch-start.png`. Other poses need visual review.
- Backup Replace roundtrip passed on the installed preview. Exported JSON was copied to `dist/qa-047-e2e/backup-before-replace-2026-09-24.json`; it contains one strength session, one stretch session, 42 set logs, and the program/routine data. Replace showed the expected summary and wrote an identical safety copy. After force-stop, History still showed the stretch entry and the 480 kg strength entry. A second export was byte-identical to the first, SHA-256 `9062e1ba4c8f38f4b51a75450560e3d37fb9dd46f44ac5e7f21219cbc87d2989`. Evidence: `108-import-summary.xml`, `109-after-replace.xml`, `110-history-after-replace-restart.xml`, `111-strength-after-replace-restart.xml`, and the two backup JSON files. Last UI position: Settings after the second export.
- Figure review captured all nine routine poses at the S25-sized emulator viewport: `95-stretch-start.png` for Elephant walks and `pose-2.png`, `pose-3.png`, `pose-stable-5.png`, `pose-7.png`, `pose-ninety-lean.png`, `pose-11.png`, `pose-13.png`, `pose-plow-hold.png` for the rest. The intended motion is recognizable, including the final 90/90 and Plow positions. The body remains a smooth pale mannequin with little muscle definition. Plow occupies little of the large white demonstration panel. Source inspection **supersedes** the initial visual suspicion about shorts length: `render.swift` paints from 13 cm above the hip landmark to 6 cm below the knee landmark, so the requested cut is implemented. Bent-pose screenshots make the hem appear near the knee; do not claim a confirmed length failure. Some fast emulator screenshots omitted controls while corresponding UI XML retained them; treat that as inconclusive rendering evidence pending real S25.
- The generic emulator showed one crash dialog for Google's Speech Recognition and Synthesis during rapid Skip actions. Logcat points to `com.google.android.tts` native code, not a Khon Fitness process crash. Evidence: `pose-5.png`, `pose-review-tts-log.txt`. Repeat on a real S25 before attributing device impact. The figure review was stopped with `am force-stop` before saving another partial routine; the prior backup and session remain protected.
- A local art update is now in the preview: directional light 900, ambient fill 90, rim 170, and Plow camera zoom 0.72 in `tools/stretch-demo/render.swift`. All nine two-view sprite sheets were regenerated. Motion check passed 6804 comparisons, highlight check 29 phases, image/camera check all nine, 23 Android unit tests, signed build, and vital lint. The APK contains the same Plow sheet checksum as source. Installed-player evidence: `115-elephant-updated.png`, `117-hip-step1-updated.png`, `120-plow-hold-updated.png`. Plow is visibly larger; torso contours are clearer but still smoother than the anatomical reference. The app was force-stopped after inspection, with no new session saved. Some headless-emulator captures intermittently omit labels while UI XML contains them, so do not treat those captures as a proven S25 rendering defect.
- Large-text audit: at 1.3×, portrait Today, Settings, History, and the stretch player remained usable, but the History date ran into "Plan". At 1.6×, the calendar's 26 dp day circles clipped two-digit dates and a generic list row truncated "Daily mobility". `ListRow` now stacks title and secondary text at 1.5× or above; History expands its calendar day circles/rows and adds a gap before Plan. Installed 1.6× rechecks passed: `128-today-font160-fixed.png`, `130-history-font160-fixed.png`, `131-stretch-font160.png`. The large Start control remains visible in portrait. The 23 tests, signed build, and vital lint passed again.
- Landscape at 1.6× is still poor in the stretch player: Start stays visible but the tall demo card is cut at the fold, so the figure/cue require scrolling. Evidence: `132-stretch-landscape-font160.png`. This is a generic emulator observation; decide a responsive two-column layout or deliberate portrait lock before release. After testing, emulator settings were restored to font scale 1.0, automatic rotation on, rotation 0; the app was force-stopped with no new session saved.
- Timer notification batch: POST_NOTIFICATIONS was granted. The interval timer showed Ready/Start/Stop in the generic notification shade; Start began the countdown, Pause and Resume changed foreground notification actions, Stop removed the notification and returned to the cardio editor. Saving recorded a 1.3-minute Norwegian 4x4 session. Evidence: `134-interval-ready.xml`, `136-timer-notification-shade.xml`, `138-timer-notification-after-start.xml`, `141-timer-session-saved.xml`. The emulator notification tree lagged actual state once; `dumpsys notification` showed the current action. One UI styling and battery behavior remain unverified.
- The Pixel launcher added both Khon widgets to its picker, and the Today widget displayed today's item and stretch status. Its Start button opened an interval timer, but the widget initially stayed on "Cardio / Start" while a session was in progress. I fixed the root cause by refreshing from app-level plan/session/stretch flows in `KhonApp.kt`, not from a Today-screen effect that can disappear on navigation. Installed preview showed "In progress / Resume" after relaunch, widget Resume opened the saved editor after an upgrade, and Discard returned the widget to "Cardio / Start". Evidence: `147-widget-added.xml`, `149-widget-in-progress.xml` before fix, `151-widget-refresh-fixed.xml`, `152-widget-resume-result.xml`, `155-widget-after-discard.xml`. The test session was discarded; the earlier saved cardio session remains. The 23 unit tests, signed build, and vital lint passed again. Last UI position: generic launcher with Today widget.
- Accessibility batch passed on 2026-09-24 (done by Claude after Codex hit its usage limit). WeekStrip and History dates are now single buttons with one full label; the History weekday header is hidden; month arrows, number steppers, and eating-window time steppers have spoken labels. The selected History date says "selected" in its label, and that label moves on tap. The emulator XML still reports `selected=false`, so a spoken TalkBack check remains. UI trees at 1.0× and 1.6×: `160-today-a11y.xml` to `165-history-a11y-font160.png`, plus `160-calendar-selected-description.xml` and `161-calendar-selection-moved.xml`. 23 tests, signed build, vital lint passed. Font scale restored to 1.0.
- Eating-window batch done on 2026-09-24. Exact reminder and boundary alarms fired on time with correct notification text, re-armed, and survived an app update. Fixed three defects: the window widget went stale after in-session changes (Glance reads now happen inside the composition, both widgets); the window widget countdown could drift 30 min (now shows a clock time); a widget request that landed on a non-Today screen fired later and opened a deleted session as a black screen (KhonNav now routes requests through Today; session screens leave when the session is gone). Time steppers now say "Opens/Closes 15 minutes earlier/later". Evidence `170-` to `188-`. Settings restored to 12:00-20:00, remind 15, off, no pending alarm; today's Open/Close window record remains. 23 tests, signed build, vital lint passed. App force-stopped on the launcher.
- Historical-data batch done on 2026-09-24. Cardio editor now shows current exercise fields plus any field the session stores; verified by removing Time from Norwegian 4x4 (saved session still showed 1.3 min), then restoring Time, Avg heart rate, Watts. Evidence `189-cardio-field-removed.xml`. The Medium_Phone_API_36.1 emulator process exited once; it was restarted headless with `-no-snapshot-save`, data and the 1080x2340 override intact. App force-stopped.
- Rotation batch done on 2026-09-24. Owner chose a portrait lock for the stretch player only. `StretchScreen.kt` requests portrait and restores the prior request on leave. Verified in landscape at 1.6x: `190-stretch-portrait-lock.png`. Emulator restored to font 1.0, auto-rotate on, rotation 0; app force-stopped.

## Combined fix round, 2026-09-24

The owner asked to merge the Codex thread's findings with Claude's and fix them all, using only Opus 5.5 subagents. Each agent worked in its own git worktree under `../kf-wt/<name>` (removed after merge), created from the uncommitted tree with the base staged in the index. Claude merged each result, verified it on the emulator, and reviewed it; an Opus reviewer checked the timer and performance diffs.

| Id | Finding | Result |
| --- | --- | --- |
| A | Stretch figure is a smooth mannequin, far from the anatomy-illustration reference | Fixed in two rounds: white illustration figure, contour, anatomical muscle borders front and back, fiber lines and rounded edges on targets. 6804 motion, 29 highlight, 9 sheet checks pass; assets +10.5%. Review sheets in `dist/art-review/` |
| B | History draws one dot per date | Fixed: up to 3 dots per session, stretch runs green |
| C | Long-press to change a day and drag to swap are missing | Fixed and checked on device, plus TalkBack actions |
| D | Template edits rewrite past days | Fixed: auto overrides, Room 3 to 4; freeze starts at first use or one year back. Checked on device |
| K | Selected date announced twice | Fixed: only the selected cell carries the state |
| F | 50 MiB software bitmap per sheet | Fixed: HARDWARE bitmap decoded off the main thread; native heap 21-25 MiB with the player open |
| G | 25 Hz demo clock | Fixed: frame-synced clock, redraw only on a new frame |
| I | Google TTS crash on rapid Skip | Not an app defect: one single cue crashes `com.google.android.tts` with SIGILL on the emulator CPU. App now coalesces cues. Recheck on S25 |
| H | Timer lost on process death | Fixed: snapshot restore after crash and force-stop; Opus review race fixed; no ghost restore |
| J | Lint error in the updater | Fixed: owner chose the fewest update steps, so the no-op `setRequestUpdateOwnership` call was removed instead of declaring the permission; full lint 0 errors |
| L | Glyph and copy accessibility | Fixed: choice rows report checked state, "No entry yet", "Tap to set hours", widget labels |
| new | Health Connect import filed a watch strength workout under Norwegian 4x4 (owner report) | Fixed: unmatched types fell back to the first cardio exercise. Strength workouts are no longer imported; HIIT, walk, and similar get no default and "Import all" skips them. 3 unit tests; real Mi Fitness import unverified on device |
| new | Stop after skipping saved a "0 done" stretch session | Fixed: a run records only after one completed stretch or 30 s |

Tests: 56 unit tests pass, signed release build, `lintVitalRelease` and full `lintRelease` pass. Evidence `200-` to `221-` in `dist/qa-047-e2e/`. The earlier "0 done · 9 skipped" row on September 24 is test data from before the fix.

## Next work batch

1. The TTS crash dialog stays unverified until a real S25 run; it came from `com.google.android.tts`, not this app.
2. Physical S25 work remains a separate gate. The uncommitted app diff was reviewed on 2026-09-24; no new release-blocking regression was found. Confirm TalkBack's selected-date announcement on the device before release.

After that, the remaining audit queue is Health Connect with a real provider; and physical S25 One UI, 120 Hz frame pacing, battery, and interruption recovery. Mark untested items **unverified**, not pass.

## Quota-safe work rule

Run one focused batch at a time. Use existing `dist/qa-047-e2e/` evidence and the report before spending tools on discovery. After any significant result, append one decision row to `dist/qa-047-e2e/decisions.tsv` and update this file immediately. Keep the last user-facing update concise: what passed or failed, what changed, and the next batch. If usage is nearly exhausted, stop testing and spend the remaining budget on this checkpoint. Do not claim the whole app is perfect or S25-certified without the physical phone.
